class MainClass
{
    def main() : int {
        writeln(Second.main());
        farzad.length = 2;
        a + 3 = 4;
        a = 12 = 34;
        return 0;
    }
}
class SecondClass {
    var test1 : int[];
    var test2 : string;
    def foo2( c : int) : int {
        return 1;
    }
    def foo1( c : int, b : string , a : boolean, x : int[]) : int {

        a = v + 3 || 12 && 65 * 78 / 43 ;
        a = 1 + 2 - 3;
        b = !x && x;
        while(a <> b)
        {
            if(-c == 2) then
            {
                writeln(x.add(a, b));
            }
            else
            {
                a = 2;
            }
        }
            return 1;
    }

}